var GetStr = artifacts.require("./GetStr.sol");

contract('GetStr', function(accounts) {
  // it("should assert true", function(done) {
  //   var test_get_str = GetStr.deployed();
  //   assert.isTrue(true);
  //   done();
  //let gs = GetStr.deployed()
  // });
  // console.log(accounts)
  // it("Should return the time", function(accounts, gs) {
  //   // assert.isTrue(true)
  //   GetStr.deployed().then(function(instance) {
  //     //console.log(instance)
  //     let time = instance.getTime()
  //     console.log(time)
  //     return time
  //   })
  // })
/*  it("Should get balance", function() {
      GetStr.deployed().then(function(instance) {
        return instance.getBalance.call()
      })
      .then(function(result) {
        console.log(result)
      })
      GetStr.deployed().then(function(instance) {
        return instance.getTime.call()
      })
      .then(function(result) {
        console.log(result)
      })
  })*/
})
